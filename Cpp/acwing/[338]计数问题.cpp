//
// Created by king on 2023/10/14.
//
#include <cstdio>
#include <algorithm>

using namespace std;

int main() {
    int a, b;
    while (scanf("%d%d", &a, &b), a, b) {
        vector<int> f(b - a + 2, 0);
        for(int i=a;)
    }
    return 0;
}